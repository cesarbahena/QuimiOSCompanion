<UserControl x:Class="QuimiOSCompanion.Views.EntregaTurnoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="15" Margin="0,0,0,10"
                BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
            <StackPanel>
                <TextBlock Text="Entrega de Turno" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <Button Content="Cargar Muestras Pendientes" Command="{Binding LoadPendingSamplesCommand}"
                        Padding="15,8" Background="#28A745" Foreground="White"
                        BorderThickness="0" HorizontalAlignment="Left" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Pending Samples List -->
            <Border Grid.Column="0" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Muestras Pendientes" FontWeight="Bold"
                               Margin="10" FontSize="14"/>

                    <ListBox Grid.Row="1" ItemsSource="{Binding PendingSamples}"
                             SelectionMode="Multiple"
                             Margin="10,0,10,10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <TextBlock>
                                        <Run Text="Folio: " FontWeight="Bold"/>
                                        <Run Text="{Binding Folio}"/>
                                        <Run Text=" - Cliente: " FontWeight="Bold"/>
                                        <Run Text="{Binding ClientId}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding ExamName}" Foreground="#666" FontSize="11"/>
                                    <TextBlock>
                                        <Run Text="Recibido: "/>
                                        <Run Text="{Binding ReceivedAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Handover Form -->
            <Border Grid.Column="2" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="15">
                <StackPanel>
                    <TextBlock Text="Crear Entrega" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

                    <TextBlock Text="Turno" Margin="0,0,0,5"/>
                    <ComboBox SelectedValue="{Binding SelectedShiftId}" Margin="0,0,0,10" Padding="5">
                        <ComboBoxItem Content="Matutino" Tag="1"/>
                        <ComboBoxItem Content="Vespertino" Tag="2"/>
                        <ComboBoxItem Content="Nocturno" Tag="3"/>
                    </ComboBox>

                    <TextBlock Text="Usuario ID" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedUserId, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10" Padding="5"/>

                    <TextBlock Text="Fecha de Entrega" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding HandoverDate}" Margin="0,0,0,10"/>

                    <TextBlock Text="Notas" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                             Height="100" TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" Margin="0,0,0,15" Padding="5"/>

                    <Button Content="Crear Entrega de Turno" Command="{Binding CreateHandoverCommand}"
                            Padding="15,10" Background="#007ACC" Foreground="White"
                            BorderThickness="0" Cursor="Hand"/>

                    <TextBlock Text="{Binding SelectedSamples.Count, StringFormat='Muestras seleccionadas: {0}'}"
                               Margin="0,10,0,0" Foreground="#666" FontSize="12"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Grid Grid.Row="1" Background="#80FFFFFF"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Procesando..." FontSize="16" Foreground="#666"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
