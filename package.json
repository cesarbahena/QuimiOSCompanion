{
  "name": "consumos-app",
  "version": "1.0.0",
  "description": "A simple web app that uses web scrapping to get data from a Web App called QUIMIOS-W plus the file system to calculate daily reagent consumption and send a post request to the Web App to update the inventory.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cesar Bahena",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "cheerio": "^1.0.0-rc.12",
    "pdf-data-parser": "^1.1.3",
    "pdf-table-extractor": "^1.0.3",
    "puppeteer": "^19.7.2",
    "querystring": "^0.2.1"
  }
}
